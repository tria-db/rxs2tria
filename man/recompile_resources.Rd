% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_suppl_resources.R
\name{recompile_resources}
\alias{recompile_resources}
\title{Reconcile and re-check a supplementary resources manifest}
\usage{
recompile_resources(suppl_res, path, rxs_images, add_new_files = FALSE)
}
\arguments{
\item{suppl_res}{An existing resources data frame.}

\item{path}{Path to a directory to scan for files, or to an existing
(non-nested) zip archive to read the file list from directly. Reading a
zip archive requires the \pkg{zip} package, and does not extract any
files---\code{fname_resource} is \code{NA} for resources found this way.}

\item{rxs_images}{A \link{QWAimages} object. Its \code{roxas_version} attribute is
used to disambiguate resource-type patterns shared between classic ROXAS
and ROXAS AI, and its \code{org_img_name}, \code{image_label}, \code{slide_label}, and
\code{woodpiece_label} columns are used to auto-populate \code{linked_label} where
appropriate.}

\item{add_new_files}{Logical; if \code{TRUE}, files found in \code{path} that
aren't declared in \code{suppl_res} are added as new, freshly-typed rows
instead of being left out. Default \code{FALSE}.}
}
\value{
A data frame, see \code{\link[=compile_resources]{compile_resources()}}. Aborts instead of
returning if \code{suppl_res} lists a file with \code{status == "ok"} that isn't
found in \code{path}.
}
\description{
Re-scans \code{path} and reconciles the result against \code{suppl_res}, an existing
manifest (e.g. the output of a previous \code{\link[=compile_resources]{compile_resources()}}/
\code{recompile_resources()} call, or one read back in from a CSV). Use this
after making manual edits and/or adding or removing files in \code{path} to
create a re-checked manifest up-to-date with the contents of \code{path}.

\code{suppl_res} is treated as the source of truth wherever it overlaps with
what's actually found in \code{path}, see \code{vignette("resources")} for details.
}
\examples{
\dontrun{
# re-run after adding more files to path: manual edits on suppl_res survive;
# add_new_files = TRUE also picks up the new ones
suppl_res <- recompile_resources(suppl_res, "path/to/submission_files",
                                 rxs_images = my_images, add_new_files = TRUE)
}
}
\seealso{
\code{\link[=compile_resources]{compile_resources()}}, \code{\link[=check_supplementary]{check_supplementary()}}, \code{vignette("resources")}
}
