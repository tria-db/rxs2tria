% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qwa_class.R
\name{QWAimages}
\alias{QWAimages}
\title{Create a QWAimages object}
\usage{
QWAimages(data, roxas_version = NULL)
}
\arguments{
\item{data}{A data frame with image-level metadata. Is aligned with the required
structure for QWAimages objects and validated against the corresponding JSON schema.}

\item{roxas_version}{The ROXAS software version: \code{"roxas"} or \code{"roxas_ai"}.
If \code{NULL} (default), inferred from the \code{software} column of \code{data}.}
}
\value{
An object of class \code{c("QWAimages", "data.frame")} with a
\code{roxas_version} attribute.
}
\description{
\code{QWAimages} is a subclass of \code{data.frame} that holds image-level metadata
for a QWA dataset. It is the required component of \link{QWAmetadata} and can be
used as input for \code{\link[=collect_raw_data]{collect_raw_data()}}.
}
\details{
Each row represents one image with its associated ROXAS (AI) output file paths,
hierarchical labels (site, tree, woodpiece, slide, image), EXIF metadata,
and ROXAS (AI) settings. It is typically constructed by \code{\link[=build_QWAimages]{build_QWAimages()}}.

The \code{roxas_version} attribute records whether the data were produced by
classic ROXAS (\code{"roxas"}) or ROXAS AI (\code{"roxas_ai"}).
}
\seealso{
\code{\link[=QWAmetadata]{QWAmetadata()}}, \code{\link[=build_QWAimages]{build_QWAimages()}}
}
