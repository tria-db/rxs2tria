% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_resources.R
\name{add_resources}
\alias{add_resources}
\title{Add supplementary resource files to a QWAmetadata object}
\usage{
add_resources(x, path, recursive = FALSE, include_unmatched = FALSE)
}
\arguments{
\item{x}{A \link{QWAmetadata} object. Its \verb{$images} component, if present, is
used to auto-fill \code{linked_label} for \code{image}- and \code{analysis}-level files.}

\item{path}{Path to a directory to scan for files.}

\item{recursive}{If \code{TRUE}, recurse into sub-directories (default \code{FALSE}).}

\item{include_unmatched}{If \code{TRUE}, files that could not be matched to a
specific type are kept with \code{resource_type = "other"} (default \code{FALSE}).}
}
\value{
The \link{QWAmetadata} object with the \verb{$resources} component updated.
}
\description{
Scans a directory and records the files it finds in the \verb{$resources}
component of a \link{QWAmetadata} object. This is the convenient entry point for
listing the supplementary files (original and annotated images, raw ROXAS
output, reference series, etc.) you wish to submit alongside the required
\code{QWAmetadata} and \code{QWAdata} files.
}
\details{
\code{add_resources()} is a thin wrapper around \code{\link[=collect_resources]{collect_resources()}}: it scans
\code{path}, appends the newly found files to any resources already stored in
\code{x$resources}, and uses the object's own \verb{$images} component as the data
structure for auto-filling \code{linked_label} on per-image resources. Call it
once per directory to build up the table across several source folders.

Only a \emph{listing} of files is produced --- the files themselves are not
modified or copied. See \code{\link[=collect_resources]{collect_resources()}} for the meaning of each column
and \code{vignette("resources")} for the full workflow, including how to review
and complete \code{linked_label} and the complete resource-type table.
}
\examples{
\dontrun{
QWA_meta <- read_QWAmetadata("output_data/my_dataset_QWAmetadata.json")
QWA_meta <- add_resources(QWA_meta, path = "raw_roxas_files", recursive = TRUE)
# review / complete the table, then persist:
QWA_meta$resources
write_QWAmetadata(QWA_meta, "output_data/my_dataset_QWAmetadata.json")
}

}
\seealso{
\code{\link[=collect_resources]{collect_resources()}}, \code{\link[=QWAmetadata]{QWAmetadata()}}
}
