% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_tria_dataset.R
\name{read_tria_dataset}
\alias{read_tria_dataset}
\title{Re-open a TRIA dataset}
\usage{
read_tria_dataset(
  dir,
  components = c("metadata", "data", "profile"),
  dataset_name = NULL
)
}
\arguments{
\item{dir}{Path to a directory holding the dataset files.}

\item{components}{Character vector selecting which components to read; any
subset of \code{c("metadata", "data", "profile")}. Defaults to all three. The
\code{"profile"} component is optional: if no profile file is present it is
skipped with a message.}

\item{dataset_name}{Optional string used to disambiguate when several
candidate files are found in \code{dir} (matched against the file names).}
}
\value{
A named list containing the requested components that were found:
\itemize{
\item \code{metadata}: a \link{QWAmetadata} object.
\item \code{data}: a \link{QWAdata} object (cells and rings).
\item \code{profiles}: a named list of \link{QWAprofile} objects, named by file (empty
if none were found). A dataset may contain more than one profile (e.g. a
sector and a band profile), hence a list.
}
}
\description{
Reads the files of a TRIA dataset --- as downloaded from the TRIA database
or written locally by the package's \verb{write_*} functions --- and
reconstructs the corresponding rxs2tria objects in a single call. Files are
located in \code{dir} by their names and read via \code{\link[=read_QWAmetadata]{read_QWAmetadata()}},
\code{\link[=read_QWAdata]{read_QWAdata()}}, and \code{\link[=read_QWAprofile]{read_QWAprofile()}}.
}
\details{
Only the QWAmetadata \code{.json} and the two QWAdata \code{.csv} files (cells and
rings) are needed to re-open a dataset; the supplementary files are not
required. This makes it possible to download a published dataset and, for
example, recompute a \link[=QWAprofile]{QWAprofile} or review the ring flags in
the flags app. See \code{vignette("reopen-dataset")} for the full workflow.
}
\examples{
\dontrun{
ds <- read_tria_dataset("path/to/downloaded_dataset")
ds$metadata
ds$data

# metadata only (e.g. to inspect the images table without loading the CSVs):
meta <- read_tria_dataset("path/to/downloaded_dataset",
                          components = "metadata")$metadata
}
}
\seealso{
\code{\link[=read_QWAmetadata]{read_QWAmetadata()}}, \code{\link[=read_QWAdata]{read_QWAdata()}}, \code{\link[=read_QWAprofile]{read_QWAprofile()}}
}
