% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-json.R
\name{resolve_refs}
\alias{resolve_refs}
\title{Inline all references in a JSON schema list}
\usage{
resolve_refs(x, base_path)
}
\arguments{
\item{x}{A schema as a nested R list.}

\item{base_path}{Directory used to resolve relative \verb{$ref} paths (from where \code{x} was loaded).}
}
\value{
A copy of \code{x} with all \verb{$ref} nodes replaced by their referenced content.
}
\description{
Recursively walks the schema list and replaces every node of the form
\code{{"$ref": "path/to/schema.json"}} with the content of the referenced file.
Relative paths are resolved against \code{base_path}; the base path is updated
when descending into a referenced file so nested refs resolve correctly.
}
\keyword{internal}
