% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_resources.R
\name{collect_resources}
\alias{collect_resources}
\title{Collect resource file information from a directory}
\usage{
collect_resources(
  path,
  append_to = NULL,
  df_structure = NULL,
  recursive = FALSE,
  include_unmatched = FALSE
)
}
\arguments{
\item{path}{Path to a directory to scan for files.}

\item{append_to}{Optional resources data frame from a previous call to
\code{collect_resources()}. When provided, the new resources are appended to it.}

\item{df_structure}{Optional data frame with label columns (e.g. from
\code{\link[=extract_data_structure]{extract_data_structure()}}). Reserved for future use to auto-populate
\code{linked_label} from matching file names.}

\item{recursive}{If \code{TRUE}, recurse into sub-directories (default \code{FALSE}).}

\item{include_unmatched}{If \code{TRUE}, resources that could not be matched
to a specific type are included as "other" (default \code{FALSE}).}
}
\value{
A \link[tibble:tibble]{tibble} with columns:
\itemize{
\item \code{resource_name}: base file name.
\item \code{resource_type}: inferred resource type string.
\item \code{linked_level}: default hierarchy level for this type (\code{"dataset"},
\code{"woodpiece"}, \code{"slide"}, \code{"image"}, or \code{"analysis"}).
\item \code{linked_label}: label of the linked entity (\code{NA}; fill in manually).
\item \code{fname_resource}: absolute file path.
}
}
\description{
Scans a directory for files and returns a data frame listing each file with
its inferred resource type. Optionally appends to an existing resources
data frame, making it easy to build a resource list incrementally from
multiple directories (e.g., processed outputs + raw ROXAS files + images).
}
\details{
Resource types are inferred from file names via \code{\link[=infer_resource_type]{infer_resource_type()}}.
See that function's documentation for the full pattern table.
}
\seealso{
\code{\link[=add_resources]{add_resources()}}, \code{\link[=infer_resource_type]{infer_resource_type()}}
}
