% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/QWAmetadata.R
\name{QWAmetadata}
\alias{QWAmetadata}
\alias{as_QWAmetadata}
\title{Create a QWAmetadata object}
\usage{
QWAmetadata(
  dataset = NULL,
  authors = NULL,
  funding = NULL,
  related = NULL,
  resources = NULL,
  sites = NULL,
  trees = NULL,
  woodpieces = NULL,
  slides = NULL,
  images = data.frame()
)

as_QWAmetadata(x)
}
\arguments{
\item{dataset}{Data frame with dataset-level metadata. Typically provided via
metadata Shiny app (\code{\link[=launch_metadata_app]{launch_metadata_app()}}).}

\item{authors}{Data frame with author information. Typically provided via
metadata Shiny app.}

\item{funding}{Data frame with funding information. Typically provided via
metadata Shiny app.}

\item{related}{Data frame with related publications or datasets. Typically
provided via metadata Shiny app.}

\item{resources}{Data frame listing all raw data files to be submitted as
part of the dataset. Typically created by \code{\link[=compile_resources]{compile_resources()}} or
\code{\link[=add_resources]{add_resources()}}.}

\item{sites}{Data frame with site-level metadata. Typically provided via
metadata Shiny app.}

\item{trees}{Data frame with tree-level metadata. Typically provided via
metadata Shiny app.}

\item{woodpieces}{Data frame with woodpiece-level metadata. Typically
provided via metadata Shiny app.}

\item{slides}{Data frame with slide-level metadata. Typically provided via
metadata Shiny app.}

\item{images}{A \link{QWAimages} object or plain data frame with image-level
metadata. Required. Typically the output of \code{\link[=build_QWAimages]{build_QWAimages()}}.}

\item{x}{A named list with any subset of the \code{\link[=QWAmetadata]{QWAmetadata()}} components.}
}
\value{
An object of class \code{QWAmetadata}.
}
\description{
\code{QWAmetadata} holds the hierarchical metadata associated with a QWA dataset,
structured by level of organisation:
\itemize{
\item \strong{\verb{$dataset}}: dataset-level information (name, description, license).
\item \strong{\verb{$authors}}: author information.
\item \strong{\verb{$funding}}: funding sources and grant numbers.
\item \strong{\verb{$related}}: related objects (publications, datasets, etc.).
\item \strong{\verb{$resources}}: included resources (data files)
\item \strong{\verb{$sites}}: site-level metadata (location, climate, etc.).
\item \strong{\verb{$trees}}: tree-level metadata (species, DBH, age, etc.).
\item \strong{\verb{$woodpieces}}: woodpiece-level metadata (disc, core, etc.).
\item \strong{\verb{$slides}}: slide-level metadata (preparation, staining, etc.).
\item \strong{\verb{$images}}: image-level metadata, including the ROXAS output file
paths, EXIF data, and ROXAS settings. This is the only required component
and is usually populated automatically by \code{\link[=build_QWAimages]{build_QWAimages()}}.
}

All components except \code{images} are optional and can be filled in later to
prepare a complete dataset submission to the TRIA database.

\code{\link[=as_QWAmetadata]{as_QWAmetadata()}} safely constructs a \link{QWAmetadata} object from a named list.
Component names are matched against the \code{\link[=QWAmetadata]{QWAmetadata()}} constructor parameters;
unknown names are dropped with a warning.
}
\seealso{
\code{\link[=QWAdata]{QWAdata()}}, \code{\link[=QWAimages]{QWAimages()}}, \code{\link[=build_QWAimages]{build_QWAimages()}}, \code{\link[=read_QWAmetadata]{read_QWAmetadata()}}
}
