% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_suppl_resources.R
\name{compile_resources}
\alias{compile_resources}
\title{Compile a supplementary resources manifest}
\usage{
compile_resources(path, rxs_images)
}
\arguments{
\item{path}{Path to a directory to scan for files, or to an existing
(non-nested) zip archive to read the file list from directly. Reading a
zip archive requires the \pkg{zip} package, and does not extract any
files---\code{fname_resource} is \code{NA} for resources found this way.}

\item{rxs_images}{A \link{QWAimages} object. Its \code{roxas_version} attribute is
used to disambiguate resource-type patterns shared between classic ROXAS
and ROXAS AI, and its \code{org_img_name}, \code{image_label}, \code{slide_label}, and
\code{woodpiece_label} columns are used to auto-populate \code{linked_label} where
appropriate.}
}
\value{
A data frame with one row per file and columns:
\itemize{
\item \code{resource_name}: name and path of the file relative to \code{path}
\item \code{description}: normally \code{NA}, can be filled manually if required to
provide justification for "other" supplementary files that should
be included in the upload
\item \code{resource_type}: inferred resource type string (see Details).
\item \code{linked_level}: hierarchy level for this type (e.g. \code{"dataset"},
\code{"woodpiece"}, or \code{"image"}).
\item \code{linked_label}: label of the linked entity, auto-filled from where
possible, otherwise \code{NA} (fill in manually if required).
\item \code{fname_resource}: absolute path to the file, or \code{NA} for a resource
read from a zip archive.
\item \code{status}, \code{note}: status is \code{"ok"}, \code{"review"}, or \code{"ignore"}, note
gives further details.
}

Returns \code{NULL} if no files are found in \code{path}.
}
\description{
Scans a directory or zip archive for supplementary resource files (e.g.
original and annotated images, raw ROXAS outputs, reference RWL series, etc.)
and returns a data frame listing each file together with its inferred
resource type and the hierarchical entity it belongs to (e.g. the
corresponding image_label or woodpiece_label; if applicable).

This function is only relevant for TRIA submissions which include supporting
materials in addition to the required \link{QWAdata} and \link{QWAmetadata} files. It
compiles a manifest of the supplementary resources---it does not move, copy,
rename, or compress any files. TRIA limits the kind of supplementary files
which can be submitted (see \code{vignette("resources")} for a full list), for any
other submitted files the contributor is required to provide a description of
what it is and why it is relevant. Thus the purpose of the manifest is to
ensure that only the \emph{right} supplementary files are included in the
submitted zip, and that these files can all be correctly identified.

The automatic inference of resource type and linked label leverages the
suffix file name conventions used by ROXAS and ROXAS AI plus the original
image file names. The results are checked for validity (e.g. if there were
files for which the resource type or label could not be inferred), with
warnings raised in case of issues. Two additional columns (\code{status}/\code{note})
are generated during the validity check and describe how the resource would
be treated upon submission (\code{"ok"}, \code{"review"}, \code{"ignore"}) and why.

Inspect the returned data frame and address any raised warnings. You may want to
manually change the contents of the
input directory (adding/removing/renaming files), or edit the data frame directly (editing the
values for \code{resource_type}, \code{linked_level}, \code{linked_label} or \code{description}).
\strong{Do not} edit the derived \code{status}/\code{note} columns.
After your edits, use \code{\link[=recompile_resources]{recompile_resources()}} to reconcile the updated
manifest against the current contents of \code{path} and re-derive the \code{status}/\code{note}
columns without losing any manual edits.

To verify a finalized manifest against a directory or
zip archive, ses \code{\link[=check_supplementary]{check_supplementary()}}. Then
save the resulting table to a CSV (e.g. \code{vroom::vroom_write()}) and submit
it alongside the supplementary zip archive.

See \code{vignette("resources")} for a worked example and the complete list of
recognised resource types.
}
\examples{
\dontrun{
suppl_res <- compile_resources("path/to/submission_files", rxs_images = my_images)
# review and make changes are required, then persist as a standalone CSV
# to submit alongside the resources zip:
vroom::vroom_write(suppl_res, "output_data/submission_files_resources.csv")
}
}
\seealso{
\code{\link[=recompile_resources]{recompile_resources()}}, \code{\link[=check_supplementary]{check_supplementary()}}, \code{vignette("resources")}
}
