% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-json.R
\name{resolve_allOfs}
\alias{resolve_allOfs}
\title{Merge all \code{allOf} compositions in a JSON schema list}
\usage{
resolve_allOfs(x)
}
\arguments{
\item{x}{A schema as a nested R list, typically after \code{\link[=resolve_refs]{resolve_refs()}}.}
}
\value{
A copy of \code{x} with all \code{allOf} arrays merged into their parent objects.
}
\description{
Recursively walks the schema list and merges every \code{allOf} array into a
single object. Properties are merged with \code{\link[=modifyList]{modifyList()}} (i.e., later entries
override earlier ones - but in the \code{rxs2tria} schemata, components should be distinct by definition).
\code{required} arrays are combined so that required fields from all subschemas are preserved.
}
\keyword{internal}
