# Read and resolve JSON Schema from file

Read and resolve JSON Schema from file

## Usage

``` r
resolve_schema(schema_path)
```

## Arguments

- schema_path:

  Path to the JSON schema file

## Value

A list containing the fully resolved schema
