# Collect raw rings output data

Read and combine data from all ring output files

## Usage

``` r
collect_rings_data(df_structure)
```

## Arguments

- df_structure:

  Dataframe containing filenames and data structure.

## Value

A dataframe with the raw data from all files combined (relevant columns
only).
