# Read already processed QWA data from CSV files

Read already processed QWA data from CSV files

## Usage

``` r
read_QWAdata(
  file_path,
  dataset_name = NULL,
  file_rings = NULL,
  file_cells = NULL
)
```
