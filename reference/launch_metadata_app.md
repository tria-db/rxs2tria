# Launch the metadata Shiny app

Launch the metadata Shiny app

## Usage

``` r
launch_metadata_app(server_run = FALSE)
```

## Arguments

- server_run:

  Logical, if TRUE, runs the app in server mode (for hosting on shiny
  server).
